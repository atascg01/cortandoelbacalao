import React from 'react'

export default function routes() {

    return (    
        <div style={{
            display: 'flex',
            justifyContent: 'center',
            alignItems: 'center',
            height: '100vh'
        }}>
            <h1>Routes!</h1> 
        </div>
        );
}
